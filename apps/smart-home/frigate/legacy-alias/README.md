# Stub Frigate — alias legacy (phase 2)

Redirige `frigate.frigate.svc.cluster.local` vers `frigate.smart-home`.

Utilisé par le widget Homepage (`http://frigate.frigate:5000`).

Voir `docs/runbooks/frigate-phase2-ns-migration.md`.
