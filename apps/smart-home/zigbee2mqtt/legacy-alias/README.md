# Stub zigbee2mqtt — alias legacy pour l'état hybride (phase 2)

Après migration vers `smart-home`, ce Service redirige
`zigbee2mqtt.zigbee2mqtt.svc.cluster.local:8080` vers le workload réel.

**Ne pas supprimer** tant qu'un client référence encore `zigbee2mqtt.zigbee2mqtt`.

Voir `docs/runbooks/zigbee2mqtt-phase2-ns-migration.md`.
